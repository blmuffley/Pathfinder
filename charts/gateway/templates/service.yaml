apiVersion: v1
kind: Service
metadata:
  name: {{ include "pathfinder-gateway.fullname" . }}
  labels:
    {{- include "pathfinder-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "pathfinder-gateway.selectorLabels" . | nindent 4 }}
